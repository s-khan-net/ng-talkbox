import { ConvDisplayPipe } from './conv-display.pipe';

describe('ConvDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
